# ============================================================
# Tramuntana Environment Variables
# Copy this file to .env.local and fill in your values
# ============================================================

# ── Privy Authentication ─────────────────────────────────────
# Get these from https://dashboard.privy.io/
# 1. Create an app → copy the App ID
# 2. Go to App Clients → copy (or create) a Client ID
#
# Without these, the app runs in DEMO MODE (mock auth)
NEXT_PUBLIC_PRIVY_APP_ID=
NEXT_PUBLIC_PRIVY_CLIENT_ID=

# ── Ethereum RPC ─────────────────────────────────────────────
# Used for reading on-chain data (vault balances, etc.)
# Default: https://eth.llamarpc.com (public, rate-limited)
# Recommended: Alchemy or Infura for production
NEXT_PUBLIC_ETH_RPC_URL=

# ── Vault Contracts (deploy before going live) ──────────────
# NEXT_PUBLIC_TRAMUNTANA_MASTER_VAULT=0x...
# NEXT_PUBLIC_TRAMUNTANA_ACCOUNTANT=0x...
# NEXT_PUBLIC_TRAMUNTANA_TELLER=0x...
